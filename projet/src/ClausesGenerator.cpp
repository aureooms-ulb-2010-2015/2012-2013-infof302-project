#include "ClausesGenerator.h"

std::vector<std::vector<int>> ClausesGenerator::run(const Problem& problem){
	
	return this->generateClauses();
}

/*
 * Returns FNC clauses
 *
 */

std::vector<std::vector<int>> ClausesGenerator::generateClauses(){
	std::vector<std::vector<int>> clauses;
	return clauses;
}

void ClausesGenerator::generateClausesRecursively(std::vector<std::vector<int>>& clauses){}
